import * as React from "react";
import { View, Text, StyleSheet } from "react-native";

export default function Lotto({ navigation }) {
  return <View>
   <Text>Lotto</Text>
   <View style={{backgroundColor: "red"}}></View>
  </View>;
}

const styles = StyleSheet.create({
  container: {
  },
});
